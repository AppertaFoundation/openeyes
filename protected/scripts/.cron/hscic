# /etc/cron.d/hscic: hscic updater
# Run every at 04:15 to pull latest ODS / HSCIC GP and practice data
# pulls in environment variables from /etc/profile or /env.sh
15 4 * * *	root	[ ${OE_ENABLE_HSCIC_IMPORT^^} = "TRUE" ] && { . /env.sh >/dev/null 2>&1 ; . /etc/profile >/dev/null 2>&1 ; bash $WROOT/protected/scripts/import-hscic-data.sh ;} || exit 0
