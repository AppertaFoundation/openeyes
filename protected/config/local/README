For local configuration, copy local.sample/common.sample.php to local/common.php and edit as required.

Web, console and test configurations may be overridden separately if required in a similar way.

common.php - Used by main, test and console
main.php - Used by main only
test.php - Used by test only
console.php - Used by console only

OpenEyes parameters (incomplete list):

specialty_codes - array of medical specialty codes to indicate what specialties should be displayed on the patient summary page
specialty_sort - array of specialty codes to provide a fixed order to episodes being displayed by specialty.
